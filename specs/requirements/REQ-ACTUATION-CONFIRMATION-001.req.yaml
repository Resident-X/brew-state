id: REQ-ACTUATION-CONFIRMATION-001
type: requirement
category: functional
title: Every commanded output is confirmed against an independent observation
status: active
priority: must
version: 1
text: |
  The controller commands heaters, pumps and valves, and can observe what followed through sensing that does not depend on the command having been believed. Confirmation is the comparison between the two.

  Outputs differ in what is observable about them. Some can be confirmed within a mains cycle; others only through a thermal or hydraulic effect, which is slower and far less specific about what actually failed.
rationale: |
  Requires the observation to be independent of the command path, because a confirmation derived from the same signal that issued the command establishes only that the controller still holds its own intention.

  Distinguishes fast direct confirmation from slow effect-based confirmation rather than demanding one mechanism everywhere. Which channels get direct sensing is a cost and hardware decision, and it is worth recording that the current parts list contains no current sensing of any kind. This requirement exists partly to make that gap visible before parts are bought rather than after.

  The ambiguity criterion is deliberate and is the opposite of what a diagnostic system is usually asked for. Effect-based confirmation genuinely cannot separate a failed switch from a failed load from a failed sensor, and reporting a specific cause the observation does not support sends the builder to the wrong place with confidence.
links:
  - target: OBL-CONTROLLER-FAULT-RESPONSE-001.C1
    type: derives-from
  - target: OBL-CONTROLLER-FAULT-RESPONSE-001.C2
    type: derives-from
  - target: OBL-CONTROLLER-FAULT-RESPONSE-001.C3
    type: derives-from
  - target: OBL-CONTROLLER-FAULT-RESPONSE-001.C4
    type: derives-from
