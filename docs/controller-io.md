# Controller I/O enumeration

**This file is local, like `reference-machine.md`.** It answers one question: what does a
controller have to provide to serve every channel this machine already senses or drives, plus
every channel the graph has since decided to add? It is written by hand in this slice —
`DEL-COMPONENT-SPECIFICATION.C6` (regenerating a specification and purchase list from the graph)
is a later criterion of the parent task and is not attempted here.

Every row traces to one of two sources: the machine's declared configuration in
[`reference-machine.md`](reference-machine.md) — sensing and actuation recorded there as already
fitted — or a decision that added a channel, named against that decision rather than appearing
without provenance. A channel is not in this enumeration unless one of those two things put it
there; a channel a requirement wants but nothing has yet fitted or decided to fit is called out as
open, not invented into a row.

What accuracy, resolution and update rate each channel needs was out of scope when
`SOL-CONTROLLER-CHANNEL-ENUMERATION` first wrote these tables, because the work those figures had
to be derived against — sensing error carried in the commanded margin — had not landed. It has
since, and `SOL-CHANNEL-MEASUREMENT-FIGURES` adds the three columns to both tables below. Still out
of scope: choosing a specific part or controller for any channel; the cost of anything; wiring,
fitting or pin assignment; and the machine's plumbing, which `DEC-PLUMBING-LEFT-AS-BUILT` already
leaves as the factory built it.

Several rows below were first written against a declaration that had not been checked against the
machine's own service manual. That document settles more than was assumed: the three temperature
sensors' element type, the two solenoid coils' voltage class, and the interface the two gauges are
actually driven through, each of which stood as *not established* here until
`SOL-CHANNEL-ENUMERATION-CORRECTED` read the primary source. The same reading found three channels
this enumeration never carried at all — a mains zero-cross detector, the operator panel's
pushbuttons, and its indicator LEDs — which are added below on the same footing as every other row, because
`reference-machine.md` now declares them. The operator-panel channels reach the main board over a
harness rather than sitting on a board of their own: `DEC-U8-BOARD-RETENTION-UNVERIFIED` once left
open whether a replacement could keep a panel board instead of driving those channels itself, and
records that the question does not arise: the panel board is retained, but it carries no controller to speak to. They are enumerated here as
channels a controller must serve directly, which is now the only way they can be served.

Five rows have since been brought current with a decision that changed what they demand. Both heater
rows and both pump rows recorded the OEM device and left this design's drive scheme *not
established*; the mains zero-cross row recorded a half-wave front end presenting one conduction
window per cycle. With actuation settled as a per-half-cycle energise-or-idle decision below the
hardware seam (`DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM`), the heaters need a device commandable every
half-cycle, which a mechanical relay is not; the pumps keep the phase-capable arrangement they
already have, which settles their rows the other way and leaves only the granularity of modulation to
measurement; and the reference needs both crossings of each cycle, which a half-wave circuit does not
give. `SOL-CONTROLLER-AND-MAINS-PARTS-DOWN-SELECT` updates those three
rows so a candidate is assessed against a live list rather than a stale one, and each records the
OEM part as the capability floor a replacement has to clear rather than as a specification this
design inherits. Those rows state their rate and resolution in supply half-cycles rather than in
milliseconds, because the quantity belongs to the supply the machine is plugged into rather than to
this design: the same rows read correctly on a 60 Hz supply, where a half-cycle is 8.33 ms and 1.2
of them fall in each 10 ms control step. Nothing depends on that count being a whole number — the
residual carried forward between half-cycles is what makes the fractional case behave as the integer
one does, and it is not an optimisation to be tidied away on a 50 Hz machine where the count happens
to come out at one.

## What the control needs of each channel

The three added columns carry no figure invented for this document. Each is read off something this
design has already declared and committed to, and each cell names which one it took and which
requirement criterion that figure serves. There are four admissible sources and no others:

- **The delivery bands**, `firmware/params/tolerance.declaration`. `brew-temperature-band` (1000
  milli-c, i.e. ±1 °C, `@document`) is how far water reaching the coffee may sit from what was
  commanded; `flow-departure-band` (200 milli-ml-s, i.e. ±0.2 mL/s, `@estimated`) is the smallest
  departure a delivery is judged against.
- **The declared sensing error**, accounted for in `firmware/params/control.declaration` and valued
  in the control sources that file accounts for. That declaration deliberately carries no numbers —
  each figure has one definition, in the source, and one account, in the declaration — so both sites
  are named here: `CONTROL_SENSING_ERROR_C` on the brew channel (value `2.0f` at
  `firmware/src/control/control.c:114`) and `STEAM_CONTROL_SENSING_ERROR_C` on the steam channel
  (value `2.0f` at `firmware/src/control/steam_control.c:63`), both `@estimated` at 2 °C — the
  accuracy class a block-mounted domestic sensor ordinarily carries — and each already the figure
  its own loop's commanded margin is widened by.
- **The states and bands the steam path is held at**,
  `firmware/params/steam_control.declaration`. `draw-pressure-floor-bar` (1000 milli-bar,
  `@estimated`) and `draw-pressure-ceiling-bar` (1400 milli-bar, `@estimated`) are the two edges of
  the pressure band a draw is held inside; `ready-pressure-bar` (900 milli-bar, `@estimated`) is the
  threshold the feed pump is withheld below, and `ready-temperature-c` (125000 milli-c,
  `@estimated`) the block temperature the loop holds while nothing is drawn.
- **The control loop's step interval**, `CONTROL_STEP_INTERVAL_MS` in
  `firmware/src/control/control.h` (10 ms, i.e. 100 Hz), accounted for as `step-interval-ms`
  `@estimated` in `firmware/params/cadence.declaration`. Every update rate below is stated as that
  interval or as a stated whole multiple of it; the full account of the interval is in *Compute and
  operator-interface demand*, further down.

What a part costs is not among them. The parent task, `DEL-COMPONENT-SPECIFICATION`, applies cost to
a specification already settled rather than letting it shape one, and
`SOL-CHANNEL-MEASUREMENT-FIGURES.C2` carries that ordering into these columns by making a figure
whose named basis is a price inadmissible. So a figure adopted because a part happened to offer it
at a price is the case this ordering exists to exclude.

Every cell carries the marking its source carries: where a declaration marks its own value
`@estimated`, so does the cell, because a figure copied across without that marking would read as
established, which is exactly the failure those declarations exist to prevent. Where no admissible
declared figure reaches a channel, the cell says so rather than carrying a plausible number, and
where the quantity does not apply to the channel at all — the accuracy of a two-state contact
closure — the cell says that instead. This is the discipline the Signal and Peripheral class
columns already apply, extended to three more columns. Naming, against each cell recorded as not
yet derivable, the work that will settle it is `DEL-COMPONENT-SPECIFICATION.C3` and is not
attempted here.

No row claims a multiple of the step interval greater than one. A slower sub-cadence for a channel
that plainly does not need reading a hundred times a second would be a figure this design has not
declared anywhere, and inventing one is the thing the ordering above rules out; so every channel is
provisioned at one control step, which is the budget the compute section below already reasons
against.

Five channels are not provisioned that way, and each says so in its own cell. Mains zero-cross detection is paced by the supply, which produces crossings whether or not a control step has come round — not a figure invented here either, but the mains frequency `reference-machine.md` § Supply already declares, and the exception exists because the quantity is an external event rate rather than a rate this design chose. The two heater mains-presence confirms are paced by the interval a command's effect is judged over, which the confirmation requirement sets rather than this document. And the low-water indicator and the buzzer are paced by the operator rather than by the loop: each has to reach somebody before they act on a machine that is not ready, which no declared figure of this design fixes and which this document does not invent one for.

## What the retrofit keeps

Each row below carries a **retention** state, because what a controller has to accommodate is not
only which channels exist but which of the devices behind them survive the retrofit.
`DEC-DEVICE-RETENTION-BOUNDARY` settles that boundary.

The distinction that makes the column useful is between three layers, not two. A **device element**
— a switch contact, an indicator element, a motor winding, a thermistor bead, a heating element, a
solenoid coil — is retained, and imposes a *characteristic* on whatever drives or reads it. The
**conditioning network** around it — the pull-ups on the buttons and the dividers on the
thermistors — sits on the main board being replaced and is therefore a design choice still open.
The indicators' series resistors and the gauges' stepper driver are the exception: those sit on
the front assemblies and stay with them, so they are inherited rather than chosen. The **carrier board** itself is replaced outright.

Reading those layers as one is what produced an earlier version of this section, which listed the button pull-ups as a retained constraint. They are not: the pull-ups are on the main board and a replacement chooses its own, so what a retained key imposes is that it is a dry contact to ground wanting a pull-up, not any particular resistance. The indicators' series resistors are the opposite case and were mis-sorted with them: those are on the panel and stay, so they are inherited.

The two front assemblies are not alike — `reference-machine.md` records both Holtek parts on the main
control board, with the button and indicator panel carrying the indicator elements, the five keys and the buzzer, and the gauge assembly carrying the two movements, the stepper driver and the low-water indicator. Only the first is passive, so a statement about "the front panel" is true of one assembly and false of the other. So for the button panel's channels the element and the main board's circuitry separate cleanly. For the gauge assembly and the indicator series resistors they do not: those circuits are on retained assemblies and are inherited.

This is also the record `OBL-PHYSICAL-CONFIGURATION-001.C1` and `.C3` ask for — what depends on a
component, and which parts are reused and therefore owe a condition check before anything relies on
them. `.C3` bites hardest not on any channel here but on the protective devices tabulated further
down, which are all retained, none characterised, and none reachable by a controller that could
notice. The record lives in this document rather than in the graph because allocation from a
requirement to a physical component is not wireable in this engine, so the enumeration is the
mechanism rather than a convenience copy of one.

## Sensing channels

| Channel | Signal | Peripheral class | Count | Retention | Safety-relevant | Accuracy | Resolution | Update rate | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Coffee thermoblock temperature sensor | Analogue resistance — **NTC thermistor** on a two-wire lead (`COFFEE NIC WIRE`, `EM70025`, service manual parts list). Beta and tolerance class are not given against that line and are not established. The base resistance is: the panel sheet reads 100 kΩ against a 1 % 10 kΩ divider, which also fixes the excitation levels below | Analogue conversion, with an excitation or divider network the controller does not itself provide. The class and the levels are both established: a 100 kΩ element against a 1 % 10 kΩ divider, whose sensitivity peaks near 85-90 °C, which is where the brew band sits | 1 | **Open** — retained or substituted turns on the element-class question `DEC-DEVICE-RETENTION-BOUNDARY` deliberately leaves unsettled | No — controller does not sit in the protective chain (see Protective devices below) | ±2 °C, `@estimated` — `CONTROL_SENSING_ERROR_C`, accounted for in `firmware/params/control.declaration` and valued `2.0f` at `firmware/src/control/control.c:114`; the same figure this loop's own commanded margin is already widened by rather than a second one derived here, so the specification and the margin cannot drift apart while both go on reading as derived from the same requirement. Serves `REQ-MEASUREMENT-001.C1`, where error eats directly into the gap between commanded target and protection trip point, and stays the one figure `.C4` requires be carried in that margin. Displaced by a bench characterisation of the part actually fitted, or by a decision settling which sensor this design specifies | ±1 °C — the `brew-temperature-band` itself (1000 milli-c, `@document`, `firmware/params/tolerance.declaration`), which is the distance a delivery is judged against. Stated as the band itself rather than as some fraction of it, because no declared figure of this design fixes how far inside a band a reading has to resolve. Serves `REQ-MEASUREMENT-001.C2`: a channel coarser than the band shows the loop no departure until the delivery has already left it, which is the control law answering an excursion after it has run rather than acting inside the timescale the disturbance sets. The band is a statement about the drink taken from the extraction literature, not a measurement of this machine | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated` in `firmware/params/cadence.declaration`. Serves `REQ-MEASUREMENT-001.C2`. The interval was chosen so the loop samples far faster than the brew path's thermal constants; whether 10 ms is sufficient for the disturbances this machine actually sees is not established | `reference-machine.md` § Already fitted |
| Steam thermoblock temperature sensor | Analogue resistance — **NTC thermistor** on a two-wire lead (`Steam SOLEPLATE THERMISTOR SR6000`, `EM70020`, service manual parts list, which names it a thermistor directly — the family rather than NTC specifically, so the class on this channel rests on that line together with the wand sensor's own `NTC` label). Its electrical figures are unrecorded on the same footing as the coffee block's | Analogue conversion, same class and same open levels as the coffee block above | 1 | **Open** — as the coffee block sensor above | No | ±2 °C, `@estimated` — `STEAM_CONTROL_SENSING_ERROR_C`, accounted for in `firmware/params/control.declaration` and valued `2.0f` at `firmware/src/control/steam_control.c:63`; the figure the steam loop's own commanded margin is widened by, and its own declaration rather than a reading of the brew channel's, so either may be revised without the other silently moving with it. Serves `REQ-MEASUREMENT-001.C1` and `.C4` on this loop. Its own account marks it the optimistic side of the estimate: the same accuracy class assumed across a wider span than it was estimated over, and an NTC's accuracy ordinarily loosens toward the top of its range. Displaced by the same two things the brew figure is | Not established. The bands in `firmware/params/tolerance.declaration` hold what is delivered to the cup — the brew-temperature band holds water reaching the coffee — and none of them is a distance the steam block's own temperature is judged by. Nor does `firmware/params/steam_control.declaration` reach it: `ready-temperature-c` is the target the loop holds this block at while nothing is drawn, and a target fixes where a block sits rather than how finely a departure from it has to be separated. Its draw-pressure band could be carried into kelvin through the pressure-per-kelvin slope, but that slope is declared in `firmware/params/thermoblock.params`, which is not an admissible source here, and the band it would convert is asserted during a draw — which is precisely when that declaration says pressure parts from the temperature that would otherwise imply it. So no admissible declared figure reaches this cell and none is written | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, on the same footing and with the same open sufficiency as the coffee block above | `reference-machine.md` § Already fitted |
| Steam wand temperature sensor (conduction disc, wand exterior) | Analogue resistance — **NTC thermistor** on a two-wire lead (`Steam Arm NTC WIRE`, `EM70024`, service manual parts list, the plainest of the three labels). Levels unrecorded, as with the other two. What the type does not settle is what this channel stands for: it is wired today to drive the milk-temperature gauge directly rather than read digitally | Analogue conversion, same class and same open levels as the two block sensors above | 1 | **Open** — as the two block sensors above | No | Not derivable, and deliberately not `STEAM_CONTROL_SENSING_ERROR_C`: that figure is declared for the steam block's own sensor, and borrowing it here would state an accuracy for a channel whose relation to the quantity it stands for is itself unestablished. `reference-machine.md` § Already fitted records this disc as a hypothesis — the lag and the relative weight of its two heat paths uncharacterised, and the O-ring's isolation not shown adequate to make this a usable milk-temperature reading at all | Not derivable, same reason. No declared band holds milk temperature: the drinking-temperature floor and ceiling in `firmware/params/tolerance.declaration` bound the drink served at the drinking point, not the exterior of a wand | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, and applies from the point this disc is read digitally at all; it drives the milk-temperature gauge directly today (Signal, above). Sufficiency of the interval is not established | `reference-machine.md` § Already fitted |
| Water level sensor (tank) — the schematic's `S6`/`WAT` | Digital — magnet/reed contact closure | Plain digital level | 1 | **Retained** | No | Not applicable — a magnet/reed contact closure has two states and no continuous value to be wrong about. What could be wrong is the level the contact changes state at, which is a property of the part fitted; no declared figure of this design bounds it, and none is invented here | Not applicable — one bit, above or below the trip level. There is no finer distinction for the channel to resolve | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`: the loop declares one cadence and this channel is sampled inside it like every other. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted |
| Flow meter (water path, suction line ahead of coffee pump) | Digital — a pulse train on a three-pin connector (`CZ1`), its signal line held up by a 2 kΩ resistor to the 5 V rail and landing on a general-purpose port pin while the three analogue inputs are occupied by the thermistors. A pull-up of that kind is fitted for an open-collector or open-drain output. This supersedes an earlier reading of the same scan which placed the pair on analogue-labelled pins. **Still open:** whether the output is open-drain or push-pull, which decides whether a 3.3 V controller re-references it with a pull-up alone or needs a level shifter; and the K-factor in pulses per litre, which no document carries | Counter or timer input capture |  1 | **Retained** | No | Not established. `firmware/params/control.declaration` declares a sensing error for the two temperature channels and none for flow, and the departure band below is a distance between commanded and delivered rate rather than a bound on the meter's own error, so it cannot be spent twice. No admissible declared figure reaches this cell | ±0.2 mL/s, `@estimated` — `flow-departure-band` (200 milli-ml-s) in `firmware/params/tolerance.declaration`, about three per cent of full pump scale, which `firmware/params/thermoblock.params` puts at 7.0 mL/s (`pump.flow_ml_per_s`, `7.0 ~ 0.6`, `@estimated`) — itself a judgement for the pump type rather than a reading of the fitted part, carried with a spread that wide because the same figure has to cover an open path and a puck at working pressure. Taken from the smallest departure a delivery is judged by rather than from what a vane meter conveniently provides, which is what `REQ-MEASUREMENT-001.C6` asks for. The band is an estimate placed between two figures neither of which has been measured — above the ripple a vane meter is expected to show on a vibratory pump, below the shortfall a choked puck is expected to produce — so it is what the design is working to, not a figure a bench has held | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted; siting decided by `DEC-FLOW-SENSED-UPSTREAM-OF-PUMP` |
| Control-knob microswitches (`S7` hot water, `S8` steam) | Digital — contact closure | Plain digital level | 2 (one per knob) | **Retained** — the switches sit on the L-panel and R-panel assemblies, reach the board on their own wires rather than through the front-panel harness, and impose a contact arrangement on whatever reads them | No | Not applicable — a contact closure has two states per knob and no continuous value to be wrong about | Not applicable — one bit per knob, closed or open | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`: the loop declares one cadence, and no declared figure of this design gives an operator input a slower one. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted |
| Operator-panel keys (`PWR`, `MAN`, `ONE`, `TWO`, `PRG`) | Digital — contact closure against a 1 kΩ pull-up, one line per button | Plain digital level | 5 keys, presenting as 5 signals. The panel carries **ten** switches, wired as five parallel pairs — a wide cap with a tact switch at either end — so the controller sees one signal per key and cannot tell which half was pressed. The circuit diagram's `S1`–`S8` is the **main board's** input list, not the panel's switch list: five arrive from this panel and three (`WAT`, `HOT`, `STM`) are the tank level sensor and the two knob microswitches, which have their own rows here and are not on the panel. See [`connector-pinouts.md`](connector-pinouts.md) § Front panel | **Retained** as switch elements on a passive panel. Their 1 kΩ series resistors stay with the panel; the pull-ups do not — those sit on the main board being replaced. What the panel imposes is a line shared with the indicator cathodes, which the controller must both sink and read | No | Not applicable — a contact closure has two states and no continuous value to be wrong about. What could be wrong is which function a given closure is taken to mean, which is a wiring and labelling question rather than an accuracy one, and the schematic's own labels are the open part of it | Not applicable — one bit per button, pressed or not. There is no finer distinction for the channel to resolve | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, on the footing the knob microswitches above carry: the loop declares one cadence and no declared figure of this design gives an operator input a slower one. The debounce interval this panel needs is a separate quantity and is not established — the schematic shows the pull-ups and no timing | `reference-machine.md` § Already fitted (circuit diagram sheet 3-3); retention settled by `DEC-DEVICE-RETENTION-BOUNDARY` |
| Mains zero-cross detection | Digital — a mains-referenced barrier part presenting both crossings of every cycle (`DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM`). The OEM front end does not: a `PC817` optocoupler fed from the line through a `1N4007` and a 120 kΩ resistor gives a half-wave conduction window, one per mains cycle rather than a symmetric edge at each crossing, and a scheme resolving half-cycles cannot be timed against a reference reporting every second one. Recorded as the capability floor a replacing front end has to clear. The relationship between a detected edge and the true crossing is not established | Digital input with edge capture on both edges and interrupt latency low enough to place a switching instant against it — the OEM routes it to a microcontroller interrupt pin rather than to a polled port, and the per-half-cycle heater drive in the actuation table raises that demand rather than relaxing it. The supply's period is measured from this channel and read as an input rather than followed as a clock: an edge arriving outside a plausible window, or not arriving at all, is a supply fault to be reported rather than smoothed over (`DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM`). The OEM front end sits on a board the project assumes replaced (both microcontrollers are on the main board, which nothing proposes retaining), so a replacing controller provides its own isolated mains-referenced front end — a dropping resistor, a rectifying diode and an optocoupler, or an equivalent barrier part, in a full-wave arrangement rather than the half-wave one. Which parts is a part-selection question and out of scope here | 1 | **Replaced** — the fitted front end presents one crossing per cycle where the actuation scheme needs both | **Yes** — not because it switches anything, which it does not, but because it is the only mains-referenced *sensing* channel in either table. `OBL-ELECTRICAL-THERMAL-SAFETY-001.C6` draws the line at whether a single insulation or clearance failure can bridge mains to the low-voltage side the controller, its sensing and the operator's controls all sit on; the `PC817`'s optical barrier is that separation on this channel, so a failure of it is exactly the case that criterion names. It is separately the phase reference a phase-controlled or burst-fired heater drive would time against, and those channels carry their own marking in the actuation table below | Not applicable as an amplitude: the reading is an edge, not a value. The quantity that can be wrong is the phase offset between the true zero crossing and the detected edge, which a diode-and-resistor front end certainly introduces and which no declared figure of this design bounds. Not established, and not invented here — and load-bearing rather than incidental now that the heater channels place their switching instants against this reference | Not applicable on the same footing — one bit, conducting or not, now taken at both crossings rather than one. The distinction that would matter is timing rather than amplitude, and no declared figure of this design fixes how finely a switching instant has to be placed against the crossing | Paced by the supply rather than by the control loop — the one row here whose cadence the loop does not set, because its events arrive when the mains produces them. Presenting both crossings doubles the event rate the OEM half-wave circuit gave: one edge per half-cycle rather than one per cycle. In absolute terms that is 100 edges per second on a 50 Hz supply and 120 on a 60 Hz one, which is why this row states the rate in half-cycles and leaves the frequency to the declaration. The 50 Hz of `reference-machine.md` § Supply is inferred from a pump nameplate, which `REQ-MACHINE-CONFIGURATION-001.C8` does not admit as the declared value, so it is provisional until measured | `reference-machine.md` § Already fitted (circuit diagram sheet 3-1); full-wave presentation required by `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` |
| Coffee element mains-presence confirm | Digital — an AC-input optocoupler (`LTV814`) fed from the switched side of the element's relay through a 120 kΩ 1 W resistor, reaching the controller on the signal tagged `CFSIG` | Plain digital level, isolated from the mains side by the optocoupler the OEM already fits | 1 | **Retained** — the sense arrangement is on the load-driving side and the isolation barrier comes with it | Yes — it is the channel that distinguishes a relay that did not close, a welded contact and an open thermal cutout from a command that was obeyed | Not applicable — this channel carries no figure of its own | Not applicable — this channel carries no figure of its own | Within the interval a command's effect is judged over, which the confirmation requirement sets rather than this document | `reference-machine.md` § Already fitted; service manual circuit diagram, sheet 3-2. Absent from this enumeration until a reading of sheet 3-2 surfaced it |
| Steam element mains-presence confirm | Digital — an AC-input optocoupler (`LTV814`) fed from the switched side of the element's relay through a 120 kΩ 1 W resistor, reaching the controller on the signal tagged `SMSIG` | Plain digital level, isolated from the mains side by the optocoupler the OEM already fits | 1 | **Retained** — the sense arrangement is on the load-driving side and the isolation barrier comes with it | Yes — it is the channel that distinguishes a relay that did not close, a welded contact and an open thermal cutout from a command that was obeyed | Not applicable — this channel carries no figure of its own | Not applicable — this channel carries no figure of its own | Within the interval a command's effect is judged over, which the confirmation requirement sets rather than this document | `reference-machine.md` § Already fitted; service manual circuit diagram, sheet 3-2. As the coffee element channel above |
| Steam-path pressure transducer | Not established — not yet physically fitted; typical transducers of this class present a ratiometric analogue output but this part has not been selected | Not established, pending part selection — analogue conversion is the expected class | 1 | **Added** — by `DEC-STEAM-PRESSURE-SENSOR-ADDED`, and not yet fitted | No — the pressure guarantee is deliberately not permitted to depend on anything noticing (`reference-machine.md` § Protection, "Pressure note") | ±0.2 bar, `@estimated` — half the 400 milli-bar between `draw-pressure-floor-bar` (1000 milli-bar) and `draw-pressure-ceiling-bar` (1400 milli-bar) in `firmware/params/steam_control.declaration`, both marked `@estimated` there and the marking carried across. The target the draw phase drives to is not declared as a figure of its own: that file states it as the middle of the band, where a loop holds with equal room either side, so an absolute error greater than half the span puts the state actually held outside the band the loop reads itself as holding. Serves `REQ-MEASUREMENT-001.C8` — pressure in the steam path measured rather than inferred from temperature — since a reading that cannot be trusted to sit inside the band it is taken to assert has stopped measuring the divergence that criterion exists for. The same file puts `ready-pressure-bar` 100 milli-bar below the floor, a tighter distance, but it is not one this channel is judged against as the design stands: the ready-holding phase drives to the block's own measured temperature, and no figure is written here for a use this design has not adopted. Displaced by a bench characterisation of the part actually fitted, or by a revision of the band | 0.4 bar, `@estimated` — the width of that same draw band, floor to ceiling, in `firmware/params/steam_control.declaration`. Stated as the band itself rather than as a fraction of it, on the footing the coffee block's temperature resolution above is stated on: no declared figure of this design fixes how far inside a band a reading has to resolve. Serves `REQ-MEASUREMENT-001.C2`: a channel coarser than the span shows the loop no departure until the pressure has already left the band, and a draw is the disturbance this loop has least room to wait out. The band is one the design can hold rather than one a drinker has asked for — that file is explicit that no bench has established what pressure a steamed jug is actually spoiled at | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, and is what the channel is provisioned against once fitted. Sufficiency of the interval is not established | Added by `DEC-STEAM-PRESSURE-SENSOR-ADDED`; not yet fitted |

**Nothing measures the brew path downstream of the coffee block** (`reference-machine.md` §
Already fitted) — that limit is inherited unchanged into this enumeration; no channel above
substitutes for it.

**The flow meter's signal is settled; its drive style and scaling are not.** An earlier reading of
the same scanned sheet put the meter's pair on pins labelled as analogue inputs, which is not where a
pulse output would land, and that reading was recorded here as a live question rather than a finding.
A closer reading retires it: the meter reaches the board on a three-pin connector, its signal line is
held up by a 2 kΩ resistor to the 5 V rail, and it lands on a general-purpose port pin, the three
analogue inputs being occupied by the thermistors. A pull-up of that kind is fitted for an
open-collector or open-drain output. The row's Signal cell now carries that reading and
`reference-machine.md` § Already fitted carries it too.

What stays open is narrower and needs a meter rather than a better scan: whether the output is
genuinely open-drain, in which case a 3.3 V controller re-references it with a pull-up alone, or
push-pull, in which case it needs a level shifter; and the K-factor in pulses per litre, which no
document carries.

## Actuation channels

| Channel | Signal | Peripheral class | Count | Retention | Safety-relevant | Accuracy | Resolution | Update rate | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Coffee thermoblock element switching | Mains AC load switch command, driven as a per-half-cycle energise-or-idle decision below the hardware seam (`DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM`). The OEM board switched this element through a mechanical relay (Hongfa `HF3FD/012HST`, 12 V DC coil, signal `CFHET`), which is on/off and nothing between and cannot be commanded once per half-cycle — recorded here as the capability floor a replacing device has to clear, not as a specification this design inherits | Digital output commandable once per supply half-cycle and timed against the zero-cross reference in the sensing table rather than free-running. Not a phase-angle output: the decision is whole-half-cycle energise or idle, so what the channel needs is a switching instant placed against a crossing rather than a firing angle within one. Which device provides it is a part-selection question and out of scope here | 1 | **Retained** element, **replaced** switching device — the element stays; the relay driving it cannot be commanded within a half-cycle | **Yes** — anything that switches a heater is in the safety-marked set | Exact as a delivered average, by construction rather than by measurement: `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` carries the residual a commanded level leaves between whole half-cycles forward rather than discarding it, so the average tracks the command with no standing offset. What that does not establish is the error inside the averaging window, which turns on the phase offset between a true crossing and a detected edge — a quantity that decision explicitly leaves unbounded and no declared figure of this design fixes | One supply half-cycle of delivered energy. That is the whole of the figure: it is stated in half-cycles rather than in milliseconds because the quantity is set by the supply the machine is plugged into, not by this design. On a 50 Hz supply a half-cycle is 10 ms and one falls in each 10 ms control step; on a 60 Hz supply it is 8.33 ms and 1.2 fall in each step. Neither ratio is relied on — `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` carries the residual forward between half-cycles, which is what makes a non-integer count per step behave exactly as an integer one does. The 50 Hz of `reference-machine.md` § Supply is a local value, and `REQ-MACHINE-CONFIGURATION-001.C8` rules a nameplate-derived frequency inadmissible as the declared one, so it stands as provisional until measured on the machine's own supply | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated` in `firmware/params/cadence.declaration` — for the commanded level, which is what `REQ-MEASUREMENT-001.C2` asks of a control law acting within the disturbance timescale. The conversion to energise-or-idle runs below the seam at the supply's own half-cycle rate, which this design does not choose and does not follow as a clock. Whether 10 ms is sufficient for the disturbances this machine actually sees is not established | `reference-machine.md` § Heating and load; admissibility of controller-driven heater channels per `DEC-HEATER-DRIVE-GATED-BY-SUPERVISION`; drive scheme per `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` |
| Steam thermoblock element switching | Mains AC load switch command, driven the same way as the coffee element — per-half-cycle energise-or-idle below the seam (`DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM`). The OEM board switched this element through the same mechanical relay `HF3FD/012HST`, signal `SMHET`, with the same reading: capability floor, not specification | Same as the coffee element channel above | 1 | **Retained** element, **replaced** switching device — as the coffee element above | **Yes** | Exact as a delivered average on the same construction as the coffee element channel, and unestablished within the averaging window for the same reason | One supply half-cycle, on the same basis and with the same supply-frequency caveat as the coffee element channel | Every control step for the commanded level, with the conversion running at the supply's half-cycle rate below the seam — identical to the coffee element channel | Same as above; drive scheme per `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` |
| Coffee pump | Modulated mains AC drive, retained rather than redesigned: the OEM scheme is a `MOC3021` opto-triac driver into a triac, signal `CFPUM`, which is phase-control capable in a way the heaters' relays are not, and `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` keeps that arrangement for both pumps. What the decision settles is that the load is modulated and by what device; what it leaves open, explicitly, is whether modulating by whole cycles is fine enough, which it calls a measurement rather than a judgement. What the OEM firmware actually did with the channel remains unknown and is not inherited | Modulated output timed against the zero-cross reference. The OEM arrangement — a `MOC3021` opto-triac driver into a triac — is already phase-capable, and `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` retains it rather than replacing it, so the controller needs a compare output able to place a firing instant within the cycle rather than a plain level. Whether modulating by whole cycles would be fine enough is a measurement that decision explicitly leaves open, so the channel is provisioned for the finer case and may turn out not to need it | 1 | **Retained**, with its phase-capable drive arrangement | No | Not established. No declared figure bounds how far the flow a commanded pump level actually produces sits from the level commanded: `firmware/params/control.declaration` declares sensing error on the two temperature channels only, and its own account of `CONTROL_DRAWN_PERMILLE_PER_K_PER_PUMP_PERMILLE` records the pump's flow-versus-level relation as an assumption no measurement has established | Fine enough to command inside the ±0.2 mL/s `flow-departure-band` (`firmware/params/tolerance.declaration`, `@estimated`), which is the band a delivery is judged by: a command step coarser than the band the loop is holding inside cannot be placed inside it. This is an actuation-side reading of that band rather than a measurement figure, and it names no requirement criterion of its own — `REQ-MEASUREMENT-001.C6` bounds what the flow *meter* resolves, not what a pump command must, and reading it as a bound on command granularity would claim more than it says. What that band is in permille of pump scale is not derivable here: the conversion runs through the flow-versus-level relation the control declaration records as unestablished | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted, § Heating and load (Ulka EP5, 48 W) |
| Steam pump | Modulated mains AC drive on the same retained arrangement, signal `SMPUM`, with the same open question about cycle granularity | Same as the coffee pump channel above — phase-capable OEM arrangement retained, provisioned for the finer case | 1 | **Retained**, as the coffee pump above | No | Not established, on the footing the coffee pump above is and with one source fewer: no band in `firmware/params/tolerance.declaration` holds the steam path's feed rate at all | Not established — the derivation available to the coffee pump above is not available here, because no declared band is a distance the steam side's feed rate is judged by. `firmware/params/steam_control.declaration` caps the level settled feed is commanded at (`sustainable-feed-rate`, 67 permille, `@estimated`) and states the interval it rises over, but a cap is a level rather than a distance, and neither fixes the step a command has to distinguish | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted, § Heating and load (JYPC-4, 22 W) |
| Two-way solenoid valve, on the coffee thermoblock (`2T0607W`) | On/off coil drive into a **mains AC** coil — the circuit diagram puts the coil in series with the line on the switched side of its relay's contacts, so whatever drives it is switching supply voltage and not a low-voltage rail. The OEM switching device is a mechanical relay (Hongfa `HF32FA/012HSL`, 12 V DC coil); the coil class is a property of the valve and survives that board's replacement, the relay is only evidence of what the board did. Coil VA is not established | Plain digital level driving a mains-rated switching device the controller does not itself provide — the coil class is now established, the device that suits it is a part-selection question and out of scope here | 1 | **Retained** — its mains-voltage coil is a constraint on whatever switches it | No — not in the explicit safety set below, though whether this or the diverter valve vents on de-energisation is an open hypothesis (`reference-machine.md` § Protection) not resolved by this enumeration | Not applicable — an on/off coil drive has two states and no continuous value to be wrong about | Not applicable — the command distinguishes energised from not, and there is no finer distinction to resolve | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`: the loop declares one cadence and this coil is commanded inside it like every other channel. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted, § Protection |
| Three-way diverter solenoid valve, near the hot water arm (`3T0618W`) | On/off coil drive into a **mains AC** coil, on the same reading and from the same sheet as the two-way valve above, and switched by the same relay part on the OEM board | Plain digital level driving a mains-rated switching device, same as the two-way valve above | 1 | **Retained** — as the two-way valve above | No | Not applicable, same reason as the two-way valve above | Not applicable, same reason | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, same footing as the two-way valve above | Same as above |
| Espresso "resistance to pour" gauge | Stepper drive, through a driver IC rather than into the windings — the motor (`MOT1`) is wired to one channel of a `VID66-08` two-channel stepper driver on the gauge assembly itself | A driver the controller **inherits** rather than brings: the `VID66-08` is on the gauge assembly, confirmed by opening the machine and reading the part off that board. The controller drives logic lines — a direction level and a step clock — rather than motor phases. The step clock wants a timer output, not a software-toggled pin: a needle has inertia and an uneven step rate reads as stutter. Those lines have a drive-level floor: the driver's datasheet puts `VIH` at a minimum of 3.15 V as an absolute figure across its whole supply range, and its own prose says sub-supply drive is what the input level shifters are for — but the shifters decouple the threshold from the motor rail without lowering it. A 5 V drive clears 3.15 V outright; a 3.3 V part qualifies only if its own `VOH` guarantee reaches that far at the current these inputs draw, and otherwise the channel carries level shifters. Retention is what imposes this, not the gauges. Retaining an out-of-production driver is an obtainability risk rather than a saving, and distinct from the unknown-condition question `OBL-PHYSICAL-CONFIGURATION-001.C3` asks, which is answered by testing the part rather than by counting how many are left in the world. See [`connector-pinouts.md`](connector-pinouts.md). What the OEM arrangement does settle is the alternative this row previously left open, since it shows the motor reached through a driver rather than by a serial link to a separate driver board | 1 | **Retained** — the motor stays and imposes its phase count and current; the driver does come with it: the `VID66-08` is on the gauge assembly rather than the main board, so a retained assembly is a retained driver | No | Not established — no declared figure of this design states how closely an operator-facing indication must follow the quantity it shows. The bands in `firmware/params/tolerance.declaration` hold the drink rather than the needle, and `firmware/params/control.declaration` declares no error for an actuation channel | Not established, but narrower than it was. The driver's microstep is settled at 1/12 degree of shaft rotation, and the driver is inherited rather than chosen, so what is open is the gearing between shaft and needle — which is a property of the retained movement and is established by counting steps between the end stops rather than by selecting a part | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`: this is the budget a step command is issued inside, not a claim about how fast a needle needs to move. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted (circuit diagram sheet 3-3); retention settled by `DEC-DEVICE-RETENTION-BOUNDARY` |
| Milk-temperature gauge | Stepper drive, same arrangement as the espresso gauge above — motor `MOT2` on the second channel of the same `VID66-08` | A second channel of the same inherited driver, on the same footing as the espresso gauge above. `MOT2` shares the OEM `VID66-08` on the gauge assembly rather than carrying a driver of its own, but that driver stays with the gauge assembly, so a replacing controller drives it rather than replacing it, on the same inherited part that serves the espresso gauge | 1 | **Retained** — the motor stays and imposes its phase count and current; the driver does come with it: the `VID66-08` is on the gauge assembly rather than the main board, so a retained assembly is a retained driver | No | Not established, same reason as the espresso gauge above, and doubly so here: the quantity this needle shows is reached through the wand disc, whose own accuracy the sensing table records as not derivable | Not established, same reason | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2`, and applies from the point a controller drives this gauge at all; the wand disc drives it directly today. Sufficiency of the interval is not established | `reference-machine.md` § Already fitted (circuit diagram sheet 3-3); retention settled by `DEC-DEVICE-RETENTION-BOUNDARY` |
| Operator-panel LED indicators | Digital — the elements are sunk on the five lines that also carry the keys, each element through a series resistor of roughly 300 Ω on the panel, **modulated rather than merely switched**: the `PWR` element ramps while the machine heats. That is produced by modulating the shared anode rail rather than any individual line, so brightness is global and a replacement needs **one** timer output for the panel rather than one per line. That rail also reaches the gauge assembly's low-water indicator over its own harness, so the one gate covers every indicator on the machine | Plain digital level on a line shared with a key pair. Whether they are driven from pins directly or through an expander or driver follows from how many pins the chosen controller has spare, which is a part-selection question and out of scope here | 8 elements across 5 functions on the panel board (`PWR` ×1, `MAN` ×2, `ONE` ×1, `TWO` ×2, `PRG` ×2). The circuit diagram's sixth function, `WATER`, is real but sits on the gauge assembly and has its own row below. The elements take no pin of their own: each group's cathodes share the line that also carries its key, and a transistor gates the common anode rail — five shared lines, not one pin per element. See [`connector-pinouts.md`](connector-pinouts.md) § Front panel | **Retained** as indicator elements on a passive panel, with their ~300 Ω series resistors, which are on the panel rather than the main board. What they impose on a replacement is the shared-line topology and a rail that has to be blanked to read the keys behind it | No | Not applicable — an element is lit or it is not, and there is no continuous value for the channel to be wrong about. Whether a lit indicator faithfully reports the state it stands for is a property of the firmware behind it rather than of this channel | Not one bit per element. The machine modulates brightness rather than only switching it — the `PWR` element ramps while it heats — so a replacement reproducing that behaviour needs a duty resolution fine enough for the ramp to read as smooth. No declared figure of this design fixes what that is, and no criterion requires the behaviour at all, nor should one -- naming a modulation scheme states mechanism. `DEC-OPERATOR-INTERFACE-REPRODUCED` is what obliges a replacement to produce it, by reference to the wiring record | Every control step — ×1 of the 10 ms (100 Hz) `step-interval-ms`, `@estimated`. Serves `REQ-MEASUREMENT-001.C2` on the footing every other actuation channel carries: the command is issued inside the one cadence the loop declares. That is the budget an update is issued in and not a claim about how fast an indicator needs to change. Forward current per element is not established — the schematic gives the series resistors without a rail this reading could pin them against | `reference-machine.md` § Already fitted (circuit diagram sheet 3-3); retention settled by `DEC-DEVICE-RETENTION-BOUNDARY` |
| Panel buzzer | Digital — one output into a `2N5401` transistor stage driving the sounder. Whether the part is a magnetic sounder, which needs a drive waveform, or a self-driving piezo, which needs only an on-off level, is **not established**, and the two ask different things of a controller | Plain digital level, or a timer output if the part turns out to need a drive waveform | 1 | **Retained** as a sounder element on the front panel; its transistor stage sits on the main board being replaced, so the drive is a fresh design choice | No | Not applicable — this channel carries no figure of its own | Not applicable — this channel carries no figure of its own | Paced by the operator rather than by the loop: it has to sound before they act on a machine that is not ready. No declared figure of this design fixes that interval | `reference-machine.md` § Already fitted; service manual circuit diagram, sheet 3-3. Absent from this enumeration until a reading of that sheet surfaced it |
| Low-water indicator | Digital — one output sinking the indicator's cathode. Its anode ties to the same `Q7`-gated rail the button panel's indicators use, reached over the gauge harness, so it is modulated with them rather than merely switched: run with a low tank, it dims and brightens in step with the `PWR` element. An earlier reading of this row had it on a separate supply, on an observation of the running machine that has since been corrected | Plain digital level | 1 | **Retained** — the element sits on the gauge assembly, which is retained | No — but it is the visual half of a low-water annunciation whose audible half is the buzzer, and the two are separable | Not applicable — this channel carries no figure of its own | Not applicable — this channel carries no figure of its own | Fast enough that the operator is told before drawing a dry shot; no declared figure of this design fixes it | `reference-machine.md` § Already fitted; harness allocation in [`connector-pinouts.md`](connector-pinouts.md). Absent from this enumeration until the gauge harness was worked out |

**On the schematic tags.** `reference-machine.md`'s parts list places the three-way valve
(`3T0618W`) near the hot water arm and the two-way valve (`2T0607W`) on the thermoblock, while the
circuit schematic drives two channels labelled `COF VALVE` and `HOT VALVE`. Which schematic tag
drives which physical valve — and therefore which of the two is the brew-and-vent valve the
Protection section's fail-safe-vent hypothesis turns on — is explicitly not established in the
source. This enumeration names the two valves by part number and known location only, and does not
guess the tag pairing.

## Protective devices (safety-relevant, not controller-addressable)

These act on temperature directly, in series with heater power, with no controller involvement
and no firmware able to reach them (`DEC-HEATER-DRIVE-GATED-BY-SUPERVISION`). They are enumerated
here — not as controller I/O, since they present no signal a controller reads or drives — because
`OBL-PHYSICAL-CONFIGURATION-001.C1` and `.C2` require every safety-relevant component identifiable
and distinguished from those whose failure does not bear on safety, and a substitution surfacing
what depended on it (`.C5`) starts from this row existing at all.

| Component | Peripheral class | Count | Retention | Safety-relevant | Source |
| --- | --- | --- | --- | --- | --- |
| Coffee thermoblock thermostat (110 °C nominal, 95–110 °C ±3) | None — electromechanical, in series with coffee heater power | 1 | **Retained** — and inherited from a machine of unknown duty, so its condition is owed before anything relies on it | **Yes** | `reference-machine.md` § Protection |
| Steam thermochamber thermostat (200 °C ±5) | None — electromechanical, in series with steam heater power | 1 | **Retained** — as the coffee thermostat above | **Yes** | `reference-machine.md` § Protection |
| Pump thermostat (110 °C) | None — electromechanical; which pump it protects is not established | 1 | **Retained** — and which pump it protects is not established, so what its retention protects is not either | **Yes** | `reference-machine.md` § Protection |
| Thermal fuse | None — electromechanical, one-shot | Fitted; exact count not established — only its mounting bracket is itemised in the parts list | **Retained** — a one-shot device of unknown age, which is the case the reused-part obligation was written for | **Yes** | `reference-machine.md` § Protection |
| Over-pressure relief, coffee side (`RELIEF ADAPTER BRASS`, fitted to the coffee thermoblock) | None — mechanical; setting and function not established | 1 | **Retained** — setting and function not established, so retention here preserves a device nobody has characterised | **Yes** — this is the device the coffee-side pressure hazard is meant to discharge through, even though what it does is not yet established | `reference-machine.md` § Protection |
| Over-pressure relief, espresso pump | None — mechanical; instruction manual claims the pump is "fitted with a pressure relief system," device/location/setting not established. May be internal to the pump, in which case it protects the pump and not the heated masses, and leaves with the pump if substituted | Not established | **Retained with the pump, and leaves with it.** If the relief is internal to the pump then substituting the pump substitutes the relief silently, which is the substitution hazard in miniature | **Yes** | `reference-machine.md` § Protection |
| Over-pressure relief, steam side | **None documented anywhere.** Neither manufacturer document lists a relief part for the steam thermochamber; its 200 °C thermostat is a temperature device and does not discharge a pressure obligation | 0 — no component fitted | Nothing to retain — none is documented | N/A — recorded as an absence, not a component, because `reference-machine.md` § Protection calls the steam-side pressure question "the largest unevidenced safety claim in the project" | `reference-machine.md` § Protection |

Every row above is **retained**, and that is the point of stating it. These are the parts `OBL-PHYSICAL-CONFIGURATION-001.C3` is written about: inherited from a machine of unknown age and duty, characteristically failing closed while appearing intact, and — unlike every channel in the two tables above — not addressable by a controller that could notice. Retention here is not a saving. It is the case where a reused part's condition has to be established before anything leans on it, and none of these has been.

Resettable vs. one-shot is not established for any of the three thermostats above, and the thermal
fuse's rating is not established either — both are recorded as open in `reference-machine.md` §
Protection and are not resolved here; they are named so a later substitution has something to
check against, per `OBL-PHYSICAL-CONFIGURATION-001.C3`.

## Compute and operator-interface demand

**Control law and estimator.** The estimator reconstructs the states the control law needs from
whatever the sensing channels above provide, produces a running comparison between predicted and
observed measurements, and delivers output at the rate the control law consumes it
(`DEL-SIM-ESTIMATOR`) — recurring, real-time arithmetic on every control tick, not a one-shot cost.
This competes with the sensing and actuation channels above for the same core and the same tick
budget.

The rate itself is a figure the control loop already commits to, not an invented one: the step
interval is declared once, at `firmware/src/control/control.h`'s `CONTROL_STEP_INTERVAL_MS` (10 ms,
i.e. 100 Hz), with its basis recorded in `firmware/params/cadence.declaration` as `@estimated` —
chosen so the loop samples far faster than the brew path's thermal time constants, which are
seconds rather than milliseconds. That file is explicit that the figure is an estimate, not a
validated sufficiency claim: "whether ten milliseconds is sufficient for the disturbances this
machine actually sees is a question for the control-law work rather than a claim made here." So
every channel above, and the estimator's own arithmetic, is provisioned against a 100 Hz tick —
each channel must be sampled, and each actuation command issued, inside that budget — while whether
10 ms is itself the right interval stays open. What stays genuinely out of scope here, per this
criterion's own boundary, is measuring the control law's actual execution time on target hardware —
that is on-target work under a later phase, and is a different question from the declared cadence
cited above.

**Operator interface.** The machine is reached one of two ways, and this enumeration states what
each spends without choosing between them:

- A colour panel driven from the controller directly, which spends the controller's own display
  drive and memory bandwidth.
- A separate display board reached over a serial link, which spends two pins (the serial
  interface) and pushes the panel's own cost onto a second part instead of the controller.

Neither option accounts for the operator surface the machine already has. The five pushbuttons, the
indicator elements and the two gauge steppers are channels in their own right in the tables above,
and on the machine as built they reach the main board over two nine-way harnesses. The button and
indicator panel is passive; the gauge assembly is not, carrying the stepper driver and the low-water
indicator. A third future was once costed here — speaking the two-wire
link between the OEM parts for two pins and keeping the panel board — and it does not exist:
`DEC-U8-BOARD-RETENTION-UNVERIFIED` records that both microcontrollers sit on the main board, so
those two wires connect two chips this project replaces rather than two boards, and the panel board is kept, but there is no controller on it to speak to. Serving these channels directly is not the conservative option; it is the only one.
The cost is seven pins for the button panel — five bidirectional lines carrying both the keys and
their indicators, a timer output gating the anode rail, and the buzzer — and six for the gauge
assembly: two step clocks, two direction lines, a driver reset and the low-water indicator. Thirteen,
not the twenty-plus a separately-driven reading gives, and a headroom argument built on this
enumeration carries that figure.

## Channels not enumerated

These are recorded as explicitly *not* fitted and *not* decided, so a reader does not mistake their
absence from the tables above for an oversight:

| Channel | Status | Source |
|---|---|---|
| Brew-path pressure transducer | Not fitted, and no decision has added one. `REQ-MEASUREMENT-001.C7` requires brew pressure measured rather than inferred from pump drive and remains open — no `addresses`, `derives-from` or `justified-by` link reaches it in the graph. `DEC-PROFILE-COMMANDS-FLOW`'s rationale previously stated as a consequence that "pressure is still measured, on both paths"; that overclaim is corrected (`SOL-BREW-PRESSURE-RATIONALE-CORRECTED`) and is not resolved by inventing a row here | `reference-machine.md` § Already fitted ("Not fitted: pressure transducer on either path"); `DEC-STEAM-PRESSURE-SENSOR-ADDED` covers the steam side only |
| Current sensing | Not fitted, no decision has added it | `reference-machine.md` § Already fitted |
| Real-time clock | Not fitted, no decision has added it | `reference-machine.md` § Already fitted |

## What depends on this enumeration

Mirrors the pattern `reference-machine.md` uses for its own declared values: if a row above
changes — a channel is added, removed, or its signal type is established — the following need
re-reading.

| This enumeration | What depends on it |
|---|---|
| Every row (existence, count, safety marking) | `OBL-PHYSICAL-CONFIGURATION-001.C1`, `.C2`; `DEL-COMPONENT-SPECIFICATION.C1` |
| The panel's key, indicator and buzzer rows | [`connector-pinouts.md`](connector-pinouts.md), which records how those channels are actually reached — five shared bidirectional lines, a gated anode rail and two buzzer conductors. A change to how the panel is wired moves that file first and these rows second |
| Every row's retention state | `DEC-DEVICE-RETENTION-BOUNDARY`, which sets it, and `OBL-PHYSICAL-CONFIGURATION-001.C1`, `.C3`, `.C5`, which need it. A channel moving between retained, replaced and added changes which of its characteristics a candidate controller must accept rather than choose |
| Every row's accuracy, resolution and update rate | `DEL-COMPONENT-SPECIFICATION.C2`; `REQ-MEASUREMENT-001.C1`, `.C2`, `.C6`, `.C8`. These cells hold no figure of their own: a change to any of the four sources they read from — the delivery bands in `firmware/params/tolerance.declaration`, the declared sensing error accounted for in `firmware/params/control.declaration` and defined in the control sources, the steam path's declared states and bands in `firmware/params/steam_control.declaration`, the step interval accounted for in `firmware/params/cadence.declaration` — moves every cell that took its figure from it |
| Compute and operator-interface demand | `DEL-COMPONENT-SPECIFICATION.C4` (headroom on the interfaces chosen) |
| Channels not enumerated | Any future decision that adds a brew-side pressure channel, current sensing, or an RTC |
| Every row's peripheral class, and the whole resource tally | [`component-specification.md`](component-specification.md), whose controller assessment counts capture channels, conversion inputs and interrupt lines straight off the peripheral-class column. A row changing its class moves that assessment, and a row added or removed moves the headroom argument with it |
| The two heater rows, the two pump rows and the mains zero-cross row | [`component-specification.md`](component-specification.md) § What each switched load needs of its device, which specifies a switching part against each of them. These five rows are the ones `DEC-MAINS-PHASED-ACTUATION-BELOW-SEAM` reached |
